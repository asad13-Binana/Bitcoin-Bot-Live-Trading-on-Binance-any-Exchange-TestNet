# Select exactly one mode-specific template:
#   .env.monitor.simulation.example -> simulation-monitor.env
#   .env.monitor.testnet.example -> testnet-monitor.env
#   .env.monitor.live.example    -> live-monitor.env
# The installer generates MONITOR_TOKEN on first install. Telegram reporting
# remains off until its separate monitor-only token and chat ID are configured.
